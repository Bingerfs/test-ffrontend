import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#FFF275',
      main: '#28AAE1',
      dark: '#1833DF',
      contrastText: "#ffff",
    },
  }
});

export default theme;
