image: node:latest

stages:
    - staging
    - production

staging:
    type: deploy
    environment:
      name: staging
    stage: staging
    script:
        - npm i
        # your build command
        - npm run build
        - npx netlify-cli deploy --site $NETLIFY_SITE_ID --auth $NETLIFY_AUTH_TOKEN --prod
    only:
        - develop

production:
    type: deploy
    environment:
      name: production
    stage: production
    script:
        - npx i
        # your build command
        - npx run build
        - npx netlify-cli deploy --site $NETLIFY_SITE_ID --auth $NETLIFY_AUTH_TOKEN --prod
    only:
        - master